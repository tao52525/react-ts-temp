import * as React from 'react'
import * as styles from './header.styl'

class Header extends React.Component {
  render() {
    return <header className={styles.name}>header</header>
  }
}

export default Header
